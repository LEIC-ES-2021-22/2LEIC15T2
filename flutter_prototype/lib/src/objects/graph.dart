import 'package:charts_flutter/flutter.dart' as charts;

class Graph {
  final String hours; //para meter 9-10
  // ignore: non_constant_identifier_names
  final int State;
  final charts.Color barColor;

  Graph(this.hours, this.State, this.barColor);
}
