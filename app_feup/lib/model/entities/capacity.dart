class Capacity {
  final int id;
  String maxCapacity; //São Strings só porque ns como meter
  String availableCapacity; // caso nõ tenhamos a capacity delas
  String occupiedCapacity;
  Capacity(
      this.id, this.maxCapacity, this.availableCapacity, this.occupiedCapacity);
}
